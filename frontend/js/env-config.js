/**
 * Set your production API host when the backend is not on the same domain.
 * Leave empty ("") for local dev or when Vercel rewrites /api and /ws to your backend.
 *
 * Example (direct backend, no rewrites):
 *   window.__API_BASE__ = "https://multu-audio-api.onrender.com";
 */
window.__API_BASE__ = window.__API_BASE__ || "";
